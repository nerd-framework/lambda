language: php
php:
  - '7.4'

env:
  global:
    - XDEBUG_MODE=coverage

install:
  - composer self-update
  - make install

script: make lint test

after_success:
  - travis_retry composer exec 'coveralls -v'
